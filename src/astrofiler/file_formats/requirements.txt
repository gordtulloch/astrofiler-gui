# Additional requirements for XISF file support
# These should be added to the main requirements.txt file

# For LZ4 compression support (optional, used by some XISF files)
lz4>=3.0.0

# Core dependencies (should already be in main requirements.txt)
# numpy>=1.20.0
# astropy>=4.0.0